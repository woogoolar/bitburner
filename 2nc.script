var x = (getServerMoneyAvailable('home'))
while((hacknet.getNodeStats(2).cores)<16) {
    if(hacknet.getCoreUpgradeCost(2)<x) {
        hacknet.upgradeCore(2)
        sleep(1000)
    }
    sleep(1000)
}
