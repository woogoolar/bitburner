var x = (getServerMoneyAvailable('home'))
while((hacknet.getNodeStats(13).ram)<64) {
    if(hacknet.getRamUpgradeCost(13)<x) {
        hacknet.upgradeRam(13)
        sleep(1000)
    }
    sleep(1000)
}
