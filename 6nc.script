var x = (getServerMoneyAvailable('home'))
while((hacknet.getNodeStats(6).cores)<16) {
    if(hacknet.getCoreUpgradeCost(6)<x) {
        hacknet.upgradeCore(6)
        sleep(1000)
    }
    sleep(1000)
}
