var x = (getServerMoneyAvailable('home'))
while((hacknet.getNodeStats(22).cores)<16) {
    if(hacknet.getCoreUpgradeCost(22)<x) {
        hacknet.upgradeCore(22)
        sleep(1000)
    }
    sleep(1000)
}
